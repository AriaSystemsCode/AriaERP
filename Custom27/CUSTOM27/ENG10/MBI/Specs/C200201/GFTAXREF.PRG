LCALIAS = ALIAS()
IF  .NOT. USED('setups')
   SELECT 0
   USE (GCDATADIR+'setups')
ENDIF
SELECT SETUPS
SET ORDER TO VARNAME
LCTAXREFE = ' '
IF SEEK('M_TAX_REFE')
   STORE MLINE(MDATA_DEF,1) TO LCTAXREFE
ENDIF
select &lcAlias
RETURN LCTAXREFE
*
