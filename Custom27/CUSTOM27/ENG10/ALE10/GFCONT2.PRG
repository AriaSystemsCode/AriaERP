LCFILE = ALIAS()
IF  .NOT. USED('STYLE')
   SELECT 0
   USE (GCDATADIR+'STYLE')
   SET ORDER TO STYLE
ENDIF
SELECT STYLE
LFCVENSTY = " "
IF SEEK(INVLINE.STYLE)
  STORE CVENSTY TO LFCVENSTY 
ENDIF
SELECT &lcFile
return LFCVENSTY
*
