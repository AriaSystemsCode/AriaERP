*LCALIAS = ALIAS()
*IF  .NOT. USED('ordline')
*   SELECT 0
*   USE (GCDATADIR+'ordline')
*   WAIT WINDOW '1'
*ENDIF
wait window '2' 
SELECT ORDLINE
wait window '3'
SET ORDER TO ORDLINES
WAIT WINDOW '4'
LCITEMNO = ' '
*IF SEEK('M_TAX_REFE')
*   STORE MLINE(MDATA_DEF,1) TO LCTAXREFE
*ENDIF
if seek(STYLE.STYLE)
 STORE ITEM_NO TO LCITEMNO
ENDIF
*select &lcAlias
RETURN LCITEMNO
*
