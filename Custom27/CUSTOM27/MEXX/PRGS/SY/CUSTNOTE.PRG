*RESTORE FROM NOTES.MEM ADDI
IF FILE('NOTES.DBF')
 IF !USED('NOTES')
   USE NOTES.DBF IN 0
 ENDIF
 REST FROM MEMO NOTES.NOTES
ENDIF
_SCREEN.VISIBLE = .F.
DO FORM (lcRpDir+lcRpMod+'\FRMNOTEPAD.Scx')
READ EVENTS 