LCFILE = ALIAS()
STORE '' TO LCCURRDESC
IF  .NOT. USED('SYCCURR')
   SELECT 0
   USE (GCSYSHOME+'SYCCURR')
ENDIF
SELECT SYCCURR
SET ORDER TO CCURRCODE
IF SEEK(RETHDR.CCURRCODE)
   STORE ALLTRIM(CCURRDESC) TO LCCURRDESC
ENDIF
SELECT &lcFile
RETURN
*
