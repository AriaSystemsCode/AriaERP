using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace AriaDevExpress.Module.BusinessObjects.WebSite
{
    public enum ComboboxEnum { None = 0, Released = 1, Hold = 2, Archived = 3, Offline = 4 };
    public enum EventStatus { Active = 0, NonActive = 1 };
}
